---
'@mikecx/thunderstorm': patch
---

Publish to npm as `@mikecx/thunderstorm` (the unscoped `thunderstorm` name was already taken). Added Changesets-based versioning/changelog and an automated GitHub Actions release workflow.
